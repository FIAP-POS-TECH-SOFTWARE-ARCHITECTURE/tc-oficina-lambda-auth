{
  "name": "tc-oficina-lambda-auth",
  "private": true,
  "scripts": {
    "build": "esbuild src/handlers/token.ts src/handlers/authorizer.ts --bundle --platform=node --target=node22 --outdir=dist --external:@aws-sdk/*",
    "test": "jest",
    "package": "npm run build && cd dist && zip -r ../lambda.zip ."
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@aws-sdk/client-ssm": "^3.700.0",
    "@types/aws-lambda": "^8.10.145",
    "@types/jest": "^30.0.0",
    "@types/jsonwebtoken": "^9.0.7",
    "@types/node": "^22.10.7",
    "@types/pg": "^8.11.10",
    "esbuild": "^0.24.0",
    "jest": "^30.0.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.3"
  }
}
