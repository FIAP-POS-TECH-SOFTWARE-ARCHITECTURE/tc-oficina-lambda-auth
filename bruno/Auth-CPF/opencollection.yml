opencollection: 1.0.0

info:
  name: Auth-CPF
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  auth:
    type: bearer
    token: "{{token}}"
  variables:
    - name: gatewayUrl
      value: ""
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
